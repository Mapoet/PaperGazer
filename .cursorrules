# PaperGazer 项目 Cursor Rules

## 项目概述
PaperGazer 是一个自动化论文监控与抓取系统，使用 Python 3.11+ 开发，主要功能包括：
- 每日自动巡检 arXiv 预印本与配置的学术期刊（支持 print / online ISSN）
- 按需抓取开放获取（OA）全文或非 OA 摘要
- 结构化存储与去重管理

## 代码风格与规范

### Python 代码规范
- **Python 版本**：Python 3.11+
- **代码格式化**：使用 `black`（line-length=100）和 `isort`
- **类型提示**：所有函数必须包含类型注解，使用 `typing` 模块
- **文档字符串**：使用 Google-style docstring
- **命名规范**：
  - 模块/包：`snake_case`
  - 类：`PascalCase`
  - 函数/变量：`snake_case`
  - 常量：`UPPER_CASE_WITH_UNDERSCORES`

### 异步编程
- 优先使用 `async/await` 进行异步 HTTP 请求
- 使用 `httpx.AsyncClient` 而非 `requests`
- 异步函数命名以 `async def` 开头，或明确标注为异步

### 错误处理
- 使用 `tenacity` 进行重试，配置指数退避
- 捕获具体异常类型，避免裸 `except`
- 记录详细错误日志，包含上下文信息

### 数据验证
- 使用 `pydantic` 进行数据模型验证
- 配置文件使用 `pydantic-settings`
- API 响应数据必须验证后再使用

## 项目结构

```
PaperGazer/
├── papergazer/          # 主包
│   ├── sources/         # 数据源模块（arXiv, Crossref, OpenAlex, Unpaywall, Europe PMC）
│   ├── store/           # 存储模块（数据库、文件系统）
│   ├── core/            # 核心逻辑（巡检、抓取、全文、图表、身份）
│   ├── analytics/       # 分析模块（引用网络、概念热度、OA 指标）
│   └── cli.py           # 命令行接口
├── configs/             # 配置文件
├── data/                # 数据目录（gitignore）
├── tests/               # 测试代码
├── docs/                # 文档
└── scripts/             # 用户脚本（巡检、分析、检索、导出）
```

## 模块职责

### sources/ 模块
- **职责**：封装各数据源的 API 调用与数据解析
- **要求**：
  - 每个数据源独立文件（`arxiv.py`, `crossref.py`, `openalex.py`, `unpaywall.py`, `europe_pmc.py`）
  - 统一返回标准化数据结构（使用 `pydantic` models）
  - 包含错误处理与重试逻辑
  - 遵守 API 使用规范（请求间隔、邮箱标识等）

### store/ 模块
- **职责**：数据持久化与文件管理
- **要求**：
  - 使用 SQLAlchemy ORM 进行数据库操作
  - 文件路径规范化（`{year}/{sanitized_id}/paper.pdf`）
  - 文件哈希计算与去重检查
  - 数据库迁移支持

### core/ 模块
- **职责**：业务流程编排
- **要求**：
  - `ingest.py`：每日巡检与增量控制 Pipeline
  - `fetch.py`：按需抓取 Pipeline（优先级：arXiv → Unpaywall → Europe PMC → Crossref）
  - `fulltext.py`：GROBID 调用与 TEI 生成（兼容文本/XML 包装）
  - `figures.py`：图表抽取流程（pdffigures2 / Table-Transformer）
  - `identity_enrich.py`：ORCID/ROR 身份标准化
  - 包含去重逻辑
  - 记录详细日志

### analytics/ 模块
- **职责**：指标与分析任务
- **要求**：
  - `citation.py`：引用网络构建（Graph）
  - `concepts.py`：概念热度统计
  - `oa.py`：OA / FAIR 指标监控
  - 提供可组合的分析结果（persist / dry-run 可配置）

### cli.py
- **职责**：命令行接口
- **要求**：
  - 使用 `typer` 框架
  - 子命令：`check`, `fetch`, `search`, `export`
  - 友好的错误提示与进度显示

## 数据库设计

### items 表
- 主键：`id` (INTEGER PRIMARY KEY)
- 唯一约束：`doi` (UNIQUE)
- 业务主键：`identifier` (arxiv_id 或 doi)
- 索引：`source`, `published_date`, `ingested_at`

### runs 表
- 记录每次巡检的检查点（`last_checkpoint`）
- 用于增量抓取

## API 使用规范

### arXiv API
- 请求间隔：≥3 秒
- 单次最多 30,000 条，每片不超过 2,000 条
- 使用 `lastUpdatedDate` 排序，本地过滤增量

### Crossref API
- 必须包含 `mailto` 参数（进入礼貌池）
- 使用 `from-index-date` 进行增量抓取
- 使用游标分页（`cursor=*`）

### Unpaywall API
- 必须包含 `email` 参数
- 查询结果可缓存（TTL 24h）

### Europe PMC API
- 无需授权，但需遵守使用规范

## 测试要求

- 单元测试：每个模块至少 80% 覆盖率
- 集成测试：测试完整 Pipeline
- 使用 `pytest` 和 `pytest-asyncio`
- Mock 外部 API 调用

## 日志规范

- 使用结构化日志（JSON 格式）
- 日志级别：DEBUG, INFO, WARNING, ERROR
- 记录关键操作：巡检开始/结束、抓取成功/失败、重试次数等

## 性能要求

- 异步 HTTP 请求，控制并发数
- 数据库批量插入与轻量迁移
- 文件下载批量处理
- 避免重复下载（文件存在性检查 + 哈希验证）
- 对长时间跨度任务进行自动分批（默认 3 天）处理

## 合规性

- **仅使用 OA 渠道**：不跨越付费墙
- **遵守 API 使用规范**：请求间隔、邮箱标识
- **数据使用声明**：仅用于个人研究，不商业用途

## 开发工作流

1. **创建功能分支**：`git checkout -b feature/xxx`
2. **编写代码**：遵循上述规范
3. **运行测试**：`pytest tests/`
4. **代码格式化**：`black . && isort .`
5. **类型检查**：`mypy papergazer/`
6. **提交代码**：使用清晰的 commit message
- 对 CLI/脚本新增参数需在 README 与相关 docs 中同步说明

## 注意事项

- 所有外部 API 调用必须包含错误处理和重试机制
- 配置文件中的敏感信息（如邮箱）不应提交到版本控制
- 数据文件（`data/`）已加入 `.gitignore`
- 保持代码文档的及时更新
- 当出现功能整合需求时优先将逻辑下沉至 `papergazer` 包内部，`scripts/` 仅保留轻量入口
- 删除或新增字段需更新 `papergazer/store/db.py` 的轻量迁移逻辑

## 参考文档

- 技术路线：`docs/TECHNICAL_ROADMAP.md`
- 需求文档：`docs/requires.md`
- API 文档：各数据源官方文档

